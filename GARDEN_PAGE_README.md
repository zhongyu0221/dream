# Garden Landing Page - 使用说明

这是一个完全模仿 [Penderecki's Garden](https://pendereckisgarden.pl/pl/dwor-mistrza) 风格的落地页。

## 📁 文件结构

```
dream/
├── app/
│   └── garden/
│       └── page.tsx          # 主页面
├── components/
│   └── garden/
│       ├── Navigation.tsx    # 导航栏组件
│       ├── Hero.tsx          # 首屏Hero区域
│       ├── Section.tsx       # 内容区块组件
│       └── Footer.tsx        # 页脚组件
└── tailwind.config.ts        # Tailwind配置
```

## 🎨 设计特点

### 配色方案
- **背景色**: `#1a1a18` (深色)
- **主文字**: `#e8e6e1` (米白色)
- **强调色**: `#d4af37` (金色)
- **次要文字**: `#b8b6b1`, `#9a9893` (灰色)

### 字体
- **标题**: Georgia, Palatino, Times New Roman (衬线字体)
- **正文**: 系统默认字体，light weight

### 动画效果
- Framer Motion 驱动的平滑动画
- 滚动视差效果
- 淡入/滑入动画
- 悬停交互效果

## 🚀 如何使用

### 1. 访问页面

访问: `http://localhost:3000/garden`

### 2. 修改内容

#### 修改导航菜单

编辑 `components/garden/Navigation.tsx`:

```typescript
const menuItems = [
  { name: '你的菜单项', href: '#section-id' },
  // 添加更多菜单项
]
```

#### 修改Hero区域

编辑 `components/garden/Hero.tsx`:

```typescript
// 修改标题
<h1>你的标题</h1>

// 修改描述文字
<p>你的描述文字</p>

// 修改按钮文字
<button>你的按钮文字</button>
```

#### 修改背景图片

在 `components/garden/Hero.tsx` 中:

```typescript
// 替换占位图片URL
<div className="absolute inset-0 bg-[url('/your-image.jpg')] bg-cover bg-center" />
```

#### 添加/修改内容区块

在 `app/garden/page.tsx` 中:

```typescript
<Section
  id="section-id"              // 锚点ID
  number="04"                  // 区块编号
  title="区块标题"              // 主标题
  subtitle="副标题"             // 副标题
  image="/your-image.jpg"      // 图片路径
  content="内容文字..."         // 内容描述
  reverse={false}              // 是否反转布局
/>
```

#### 修改页脚

编辑 `components/garden/Footer.tsx` 中的内容。

## 🖼️ 添加图片

### 方法1: 使用本地图片

1. 将图片放入 `public/` 目录
2. 在组件中使用: `/your-image.jpg`

### 方法2: 使用外部URL

直接在组件中使用完整URL:
```typescript
image="https://example.com/image.jpg"
```

### 推荐图片尺寸

- **Hero背景**: 1920x1080px 或更大
- **Section图片**: 800x600px 或更大
- **格式**: JPG, PNG, WebP

## 🎯 自定义样式

### 修改颜色

在 `tailwind.config.ts` 或组件中直接修改颜色类:

```typescript
// 金色强调色
text-[#d4af37]  // 改为你的颜色
bg-[#d4af37]    // 改为你的颜色

// 背景色
bg-[#1a1a18]    // 改为你的颜色
```

### 修改字体大小

在组件中修改 Tailwind 类:

```typescript
text-5xl  // 改为 text-6xl, text-4xl 等
```

### 修改间距

```typescript
py-24    // 垂直内边距
px-6     // 水平内边距
gap-12   // 元素间距
```

## ✨ 动画自定义

### 修改动画速度

在 Framer Motion 组件中:

```typescript
transition={{ duration: 0.8 }}  // 修改数值 (秒)
```

### 修改动画延迟

```typescript
transition={{ delay: 0.3 }}  // 修改数值 (秒)
```

### 禁用动画

移除 `motion.` 前缀，使用普通 `div`:

```typescript
// 从
<motion.div>...</motion.div>

// 改为
<div>...</div>
```

## 📱 响应式设计

页面已包含响应式设计:

- **移动端**: `< 1024px` - 单列布局
- **桌面端**: `>= 1024px` - 双列布局

使用 Tailwind 响应式类:

```typescript
text-4xl md:text-5xl  // 移动端4xl，桌面端5xl
```

## 🔧 技术栈

- **框架**: Next.js 14 (App Router)
- **样式**: Tailwind CSS
- **动画**: Framer Motion
- **语言**: TypeScript

## 📝 内容修改清单

修改以下内容以适配你的项目:

- [ ] Hero区域标题和描述
- [ ] Hero背景图片
- [ ] 导航菜单项
- [ ] Section内容 (标题、描述、图片)
- [ ] 页脚信息
- [ ] 颜色方案 (如需要)
- [ ] Logo和品牌名称

## 🎨 设计参考

所有设计元素参考自:
https://pendereckisgarden.pl/pl/dwor-mistrza

## 💡 提示

1. **图片优化**: 使用 Next.js Image 组件优化图片加载
2. **性能**: 大量图片时考虑懒加载
3. **SEO**: 添加适当的 meta 标签
4. **无障碍**: 确保颜色对比度符合WCAG标准

## 🐛 常见问题

### 图片不显示?

- 检查图片路径是否正确
- 确保图片在 `public/` 目录中
- 检查控制台是否有CORS错误

### 动画不流畅?

- 检查是否有性能问题
- 减少同时运行的动画数量
- 使用 `will-change` CSS属性优化

### 移动端布局问题?

- 检查 Tailwind 响应式类
- 测试不同屏幕尺寸
- 调整断点值

