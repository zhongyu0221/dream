# 电影级视觉效果实现指南

本指南详细说明如何实现参考网站的电影级视觉效果。

## 🎬 已实现的效果

### 1. 胶片颗粒 (Film Grain)
- **位置**: 全局固定层，覆盖整个页面
- **实现**: CSS SVG 噪声滤镜 + 动画
- **文件**: `components/garden/FilmGrain.tsx`
- **样式类**: `.film-grain`

**特点**:
- 使用 SVG `feTurbulence` 生成噪声
- 8秒循环动画，模拟真实胶片颗粒
- `mix-blend-mode: overlay` 混合模式
- 透明度 15%，不干扰内容

### 2. 光晕/发光效果 (Bloom/Glow)
- **实现**: CSS `filter` + `drop-shadow`
- **样式类**: `.bloom-gold`, `.bloom-effect`

**应用位置**:
- 标题文字 (`bloom-gold`)
- 按钮 (`bloom-effect`)
- 重要元素

**CSS实现**:
```css
.bloom-gold {
  filter: drop-shadow(0 0 20px rgba(212, 175, 55, 0.5))
          drop-shadow(0 0 40px rgba(212, 175, 55, 0.3))
          drop-shadow(0 0 60px rgba(212, 175, 55, 0.1));
}
```

### 3. 柔光叠加 (Soft Light Overlay)
- **实现**: CSS `mix-blend-mode: soft-light`
- **样式类**: `.soft-light-overlay`

**效果**:
- 创建柔和的"光子感"
- 金色渐变叠加
- 增强画面氛围

### 4. 暗角效果 (Vignette)
- **实现**: CSS 径向渐变
- **样式类**: `.vignette`

**特点**:
- 边缘逐渐变暗
- 中心保持明亮
- 增强视觉焦点

### 5. 电影级调色 (Cinematic Color Grading)
- **实现**: CSS `filter`
- **样式类**: `.cinematic-grade`, `.cinematic-grade-strong`

**滤镜组合**:
- `contrast(1.15)` - 增强对比度
- `brightness(0.9)` - 降低亮度
- `saturate(0.85)` - 降低饱和度
- `sepia(0.1)` - 轻微复古色调

### 6. 光子感光层 (Photon Layer)
- **实现**: 多层径向渐变 + `mix-blend-mode: screen`
- **样式类**: `.photon-layer`

**效果**:
- 模拟光线照射
- 金色光点效果
- 增强画面深度

## 📁 文件结构

```
dream/
├── app/
│   └── globals.css                    # 所有视觉效果CSS
├── components/
│   └── garden/
│       ├── FilmGrain.tsx              # 胶片颗粒组件
│       ├── Hero.tsx                   # Hero区域（应用所有效果）
│       ├── Section.tsx                # Section（应用部分效果）
│       └── CinematicWrapper.tsx       # 电影级包装器
```

## 🎨 使用方法

### 在组件中应用效果

#### 1. 添加胶片颗粒（全局）
```tsx
import FilmGrain from '@/components/garden/FilmGrain'

export default function Page() {
  return (
    <main>
      <FilmGrain /> {/* 添加在页面最外层 */}
      {/* 其他内容 */}
    </main>
  )
}
```

#### 2. 应用光晕效果
```tsx
<h1 className="bloom-gold">标题文字</h1>
<button className="bloom-effect">按钮</button>
```

#### 3. 应用电影级调色
```tsx
<div className="cinematic-grade">
  <img src="image.jpg" />
</div>
```

#### 4. 完整效果组合
```tsx
<div className="cinematic-grade-strong vignette soft-light-overlay photon-layer">
  {/* 图片或内容 */}
</div>
```

## 🎬 Hero区域完整实现

Hero区域集成了所有效果：

```tsx
<div className="cinematic-grade-strong">
  {/* 基础图片 */}
  <div className="bg-[url('/image.jpg')]" />
  
  {/* 暗色渐变 */}
  <div className="bg-gradient-to-b from-[#1a1a18]/70..." />
  
  {/* 柔光叠加 */}
  <div className="soft-light-overlay" />
  
  {/* 暗角 */}
  <div className="vignette" />
  
  {/* 光子感光层 */}
  <div className="photon-layer" />
</div>
```

## ⚙️ 自定义调整

### 调整胶片颗粒强度
在 `globals.css` 中修改：
```css
.film-grain {
  opacity: 0.15; /* 改为 0.1-0.3 之间 */
}
```

### 调整光晕强度
```css
.bloom-gold {
  filter: drop-shadow(0 0 20px rgba(212, 175, 55, 0.5)) /* 调整透明度 */
          drop-shadow(0 0 40px rgba(212, 175, 55, 0.3))
          drop-shadow(0 0 60px rgba(212, 175, 55, 0.1));
}
```

### 调整调色强度
```css
.cinematic-grade {
  filter: 
    contrast(1.15)    /* 对比度 */
    brightness(0.9)    /* 亮度 */
    saturate(0.85)    /* 饱和度 */
    sepia(0.1);       /* 复古度 */
}
```

### 调整暗角强度
```css
.vignette::before {
  background: radial-gradient(
    ellipse at center,
    transparent 0%,
    transparent 40%,
    rgba(0, 0, 0, 0.3) 70%,  /* 调整这些值 */
    rgba(0, 0, 0, 0.6) 100%
  );
}
```

## 🎯 性能优化

### 1. 使用CSS而非Canvas
- 所有效果都使用CSS实现
- 性能更好，GPU加速
- 无需JavaScript计算

### 2. 合理使用混合模式
- `mix-blend-mode` 会触发重绘
- 只在必要时使用
- 避免嵌套过多混合模式

### 3. 动画优化
- 使用 `transform` 和 `opacity`
- 避免动画 `width`、`height`
- 使用 `will-change` 提示浏览器

## 📊 效果层级

```
页面层级（从下到上）:
1. 背景图片
2. 电影级调色滤镜
3. 暗色渐变遮罩
4. 柔光叠加层
5. 光子感光层
6. 暗角效果
7. 内容层
8. 胶片颗粒（最上层，全局）
```

## 🎨 颜色方案

所有效果都基于以下颜色：

- **金色**: `#d4af37` (主强调色)
- **浅金色**: `#c9a961` (次要强调)
- **背景**: `#1a1a18` (深色背景)
- **文字**: `#e8e6e1` (米白色)

## 💡 最佳实践

1. **适度使用**: 不要过度应用所有效果
2. **性能优先**: 在低端设备上考虑禁用某些效果
3. **可访问性**: 确保文字对比度符合WCAG标准
4. **测试**: 在不同浏览器和设备上测试效果

## 🔧 故障排除

### 效果不显示？
- 检查CSS类名是否正确
- 确认 `globals.css` 已导入
- 检查浏览器是否支持相关CSS特性

### 性能问题？
- 减少同时使用的效果数量
- 使用 `will-change` 优化动画
- 考虑使用 `transform` 替代其他属性

### 效果太强/太弱？
- 调整透明度值
- 修改滤镜参数
- 调整混合模式

## 📚 参考资源

- [CSS Blend Modes](https://developer.mozilla.org/en-US/docs/Web/CSS/mix-blend-mode)
- [CSS Filters](https://developer.mozilla.org/en-US/docs/Web/CSS/filter)
- [Framer Motion](https://www.framer.com/motion/)

